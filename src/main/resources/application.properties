spring.datasource.url=jdbc:mysql://localhost:3306/jobtracker
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8082
spring.jpa.open-in-view=false

logging.level.root=INFO
logging.level.com.omkar.jobtracker=DEBUG

# ===============================
# LOGGING CONFIG (DAY 16)
# ===============================

# Show SQL queries (very important for debugging DB)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Reduce Spring noise logs
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Your app logs (full debug)
logging.level.com.omkar.jobtracker=DEBUG
